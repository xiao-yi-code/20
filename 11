int main()
{
	  int *p,i,n;
	  printf("enter n:");
	  scanf("%d",&n);
	  p=(int*)malloc(sizeof(int)*n);
      for(i=0;i<n;i++)
	  {
	     p[i]=i;
	     printf("%4d",p[i]);
	  }
	  free(p);

   return 0;
}

